name: Spawn Legion Swarm (T3 Nodes)

on:
  workflow_dispatch:
    inputs:
      node_count:
        description: 'Number of nodes (Max 20 for Free Tier)'
        required: true
        default: '5'

jobs:
  deploy_swarm:
    runs-on: ubuntu-latest
    strategy:
      # [회색지대 전술] Matrix Strategy를 악용(?)하여 
      # 무료 컴퓨터 여러 대를 동시에 병렬로 기동시킴
      matrix:
        node_index: [1, 2, 3, 4, 5] 

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: |
          # 실제 연산에 필요한 라이브러리 설치 단계
          pip install requests

      - name: 🔥 Ignite T3 Node
        run: |
          # 아까 만든 두뇌 파일을 클라우드에서 실행
          python legion_node.py
name: Spawn Legion Swarm

on:
  workflow_dispatch:
    inputs:
      node_count:
        description: 'Number of nodes to spawn'
        required: true
        default: '1'

jobs:
  spawn_node:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # 입력받은 수만큼 복제 (여기서는 간단히 1~5개로 제한)
        run_id: [1] 
    steps:
      - name: Bootstrapping T3 Node
        run: |
          echo "⚡ LEGION Node-${{ matrix.run_id }} is booting..."
          echo "Hardware Profile: 2-Core CPU, 7GB RAM (Standard GitHub Runner)"
          echo "Connecting to Overmind..."
          sleep 2
          echo "✅ Connection Established. Ready for orders."

